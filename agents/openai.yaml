interface:
  display_name: "Meteor Image"
  short_description: "Generate images through a meteor041 OpenAI proxy"
  default_prompt: "Use $meteor-image to generate or edit an image through the configured meteor041-compatible proxy."

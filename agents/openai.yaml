interface:
  display_name: "Meteor041 ImageGen"
  short_description: "Generate images through a meteor041 OpenAI proxy"
  default_prompt: "Use $meteor041-imagegen to generate or edit an image through the configured meteor041-compatible proxy."
